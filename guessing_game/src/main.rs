use std::io;
use rand::Rng;

fn os_random() {
    let random_number = rand::thread_rng().gen_range(1..=200);
    println!("The random number generated by OS is {random_number}");
}

fn guess() {
    println!("Guess your favourite number!!!::!!!)");
    let mut guess = String::new();
    // io::stdin().read_line(&mut guess).expect("Failed to read the message");
    io::stdin().read_line(&mut guess).unwrap();
    println!("You guessed {guess}");
}



fn main() {
    println!("Welcome to Guess the Number Game::)");
    os_random();
    guess();
}
